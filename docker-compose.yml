version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - webapp-db:/app/db
      - static-files:/app/staticfiles
      - media-files:/app/media
    container_name: webapp-container

volumes:
  webapp-db:
  static-files:
  media-files:
  